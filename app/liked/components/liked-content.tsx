'use client'

import { Song } from '@/types'

interface LikedContentProps {
  songs: Song[]
}

export const LikedContent = (props: LikedContentProps) => {
  const { songs } = props

  return <div></div>
}
